/* LinkFeature — anchor colour inside the page.
   Migrated from link.css into the Emotion Global skin (aggregated by src/editor/skin.tsx). */
import { css } from '@emotion/react'

export const linkStyles = css`
  .document-editor__surface a {
    color: var(--editor-accent-ink);
  }
`
